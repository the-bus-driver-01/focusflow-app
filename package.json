{
  "name": "tailwind-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo with Tailwind CSS configuration",
  "scripts": {
    "dev": "pnpm --filter ./apps/* dev",
    "build": "pnpm --filter ./apps/* build",
    "lint": "pnpm --filter ./apps/* lint"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "postcss": "^8.4.35",
    "autoprefixer": "^10.4.17"
  }
}
